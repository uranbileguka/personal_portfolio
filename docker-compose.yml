version: "3.9"

services:
  # ===================== REACT APP =====================
  react:
    build:
      context: ./reactapp
      dockerfile: Dockerfile
    ports:
      - "8080:80"
    labels:
      - "traefik.enable=true"

      # HTTPS router
      - "traefik.http.routers.react.rule=Host(`uranbileg.dev`) || Host(`www.uranbileg.dev`)"
      - "traefik.http.routers.react.entrypoints=websecure"
      - "traefik.http.routers.react.tls.certresolver=le"
      - "traefik.http.services.react.loadbalancer.server.port=80"

      # HTTP router -> redirect to HTTPS (Traefik side will honor this)
      - "traefik.http.routers.react-http.rule=Host(`uranbileg.dev`) || Host(`www.uranbileg.dev`)"
      - "traefik.http.routers.react-http.entrypoints=web"
      - "traefik.http.routers.react-http.middlewares=redirect-to-https@docker"

      # define the redirect middleware (lives in @docker)
      - "traefik.http.middlewares.redirect-to-https.redirectscheme.scheme=https"
    networks: [proxy]
    restart: unless-stopped

  # ===================== POSTGRES (DB for Odoo) =====================
  db:
    image: postgres:16
    container_name: odoo-db
    environment:
      POSTGRES_DB: odoo
      POSTGRES_USER: odoo
      POSTGRES_PASSWORD: admin
    volumes:
      - db_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL","pg_isready -U odoo -d odoo"]
      interval: 10s
      timeout: 5s
      retries: 10
    networks: [proxy]
    restart: unless-stopped

  # ===================== ODOO 18 =====================
  odoo:
    image: odoo:18
    container_name: odoo18
    depends_on:
      db:
        condition: service_healthy
    environment:
      HOST: db
      USER: odoo
      PASSWORD: admin
      ADMIN_PASSWORD: admin
      HOME: /var/lib/odoo
      XDG_DATA_HOME: /var/lib/odoo/.local/share
      XDG_CACHE_HOME: /var/lib/odoo/.local/cache
      XDG_CONFIG_HOME: /var/lib/odoo/.config  
    volumes:
      - /home/ubuntu/apps/odoo18/config/odoo.conf:/etc/odoo/odoo.conf:ro
      - /home/ubuntu/apps/odoo18/addons:/mnt/extra-addons
      - odoo_data:/var/lib/odoo 
    labels:
      - "traefik.enable=true"
      - "traefik.docker.network=proxy" 
      # Odoo main app (HTTPS)
    # Service (unique name)
      - "traefik.http.services.odoo-svc.loadbalancer.server.port=8069"
      - "traefik.http.services.odoo-svc.loadbalancer.server.scheme=http"

    # HTTPS router (unique name)
      - "traefik.http.routers.odoo-secure.rule=Host(`erp.uranbileg.dev`)"
      - "traefik.http.routers.odoo-secure.entrypoints=websecure"
      - "traefik.http.routers.odoo-secure.tls=true"
      - "traefik.http.routers.odoo-secure.tls.certresolver=le"
      - "traefik.http.routers.odoo-secure.service=odoo-svc"

    # HTTP → HTTPS (optional)
      - "traefik.http.routers.odoo-http.rule=Host(`erp.uranbileg.dev`)"
      - "traefik.http.routers.odoo-http.entrypoints=web"
      - "traefik.http.routers.odoo-http.middlewares=redirect-to-https@docker"

    networks: [proxy]
    restart: unless-stopped

  # ===================== GPS BACKEND (Django) =====================
  gps_backend:
    build:
      context: ./se_gps/backend_gps
      dockerfile: Dockerfile
    container_name: gps_backend
    ports:
      - "8000:8000"
    environment:
      - PYTHONUNBUFFERED=1
      - DEBUG=False
      - ALLOWED_HOSTS=fleet.uranbileg.dev,localhost,127.0.0.1,gps_backend
    volumes:
      - gps_static:/app/staticfiles
    command: >
      sh -c "python manage.py migrate &&
             python manage.py collectstatic --noinput &&
             python manage.py runserver 0.0.0.0:8000"
    networks: [proxy]
    restart: unless-stopped

  # ===================== GPS FRONTEND (React) =====================
  gps_frontend:
    build:
      context: ./se_gps/frontend_gps
      dockerfile: Dockerfile
    container_name: gps_frontend
    depends_on:
      - gps_backend
    labels:
      - "traefik.enable=true"
      - "traefik.docker.network=proxy"
      
      # Service
      - "traefik.http.services.gps-svc.loadbalancer.server.port=80"
      
      # HTTPS router
      - "traefik.http.routers.gps-secure.rule=Host(`fleet.uranbileg.dev`)"
      - "traefik.http.routers.gps-secure.entrypoints=websecure"
      - "traefik.http.routers.gps-secure.tls=true"
      - "traefik.http.routers.gps-secure.tls.certresolver=le"
      - "traefik.http.routers.gps-secure.service=gps-svc"
      
      # HTTP → HTTPS
      - "traefik.http.routers.gps-http.rule=Host(`fleet.uranbileg.dev`)"
      - "traefik.http.routers.gps-http.entrypoints=web"
      - "traefik.http.routers.gps-http.middlewares=redirect-to-https@docker"
      
    networks: [proxy]
    restart: unless-stopped

volumes:
  db_data:
  odoo_data:
  gps_static:

networks:
  proxy:
    external: true

